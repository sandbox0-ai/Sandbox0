{{- if .Values.secrets.initUserPassword }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "edge-gateway.fullname" . }}-init
  labels:
    {{- include "edge-gateway.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.secrets.initUserPassword | quote }}
{{- end }}
