apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "netd.fullname" . }}-config
  labels:
    {{- include "netd.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
