apiVersion: v1
kind: Service
metadata:
  name: {{ include "internal-gateway.fullname" . }}
  labels:
    {{- include "internal-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "internal-gateway.selectorLabels" . | nindent 4 }}
